## Packages
recharts | Dashboard analytics charts for admin view
framer-motion | Smooth transitions for modals and page loads
date-fns | Formatting timestamps in tables and chat
clsx | Utility for conditional class names (usually included but good to ensure)
tailwind-merge | Utility for merging tailwind classes (usually included)

## Notes
- Dark mode is default (forced via HTML class)
- Auth flow uses /api/login and /api/user for session persistence
- Dashboard layout changes based on user.role (admin vs designer vs support)
- Recharts used for Admin finance view
